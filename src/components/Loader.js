import React from 'react';

const Loader = () => {
    return (
        <div className="ui middle aligned center aligned grid hmy-loader">
            <div className="column">
                <div className="ui active centered inline huge text loader">Loading...</div>
            </div>
        </div>
    );
}

export default Loader;
